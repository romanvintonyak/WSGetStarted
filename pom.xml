<?xml version="1.0"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">    <modelVersion>4.0.0</modelVersion>    <groupId>com.osf.romanvintonyak</groupId>    <artifactId>WSDummy</artifactId>    <version>1.0.0-SNAPSHOT</version>    <packaging>war</packaging>    <name>WSGetStarted</name>    <description>Get familiar with Web service</description>    <properties>        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>        <version.wildfly.maven.plugin>1.0.2.Final</version.wildfly.maven.plugin>        <!-- other plugin versions -->        <version.war.plugin>2.5</version.war.plugin>        <version.compiler.plugin>3.1</version.compiler.plugin>        <!-- maven-compiler-plugin -->        <maven.compiler.target>1.7</maven.compiler.target>        <maven.compiler.source>1.7</maven.compiler.source>        <!-- Remote Server URL -->        <remote.server.url>http://localhost:8080/</remote.server.url>        <wildfly.hostname>localhost</wildfly.hostname>        <wildfly.username>roman</wildfly.username>        <wildfly.password>roman</wildfly.password>        <mysql.driver.groupId>mysql</mysql.driver.groupId>        <mysql.driver.artifactId>mysql-connector-java</mysql.driver.artifactId>        <mysql.driver.version>5.1.25</mysql.driver.version>    </properties>    <dependencies>        <dependency>            <groupId>javax.persistence</groupId>            <artifactId>persistence-api</artifactId>            <version>1.0.2</version>        </dependency>        <dependency>            <groupId>${mysql.driver.groupId}</groupId>            <artifactId>${mysql.driver.artifactId}</artifactId>            <version>${mysql.driver.version}</version>        </dependency>        <dependency>            <groupId>javax</groupId>            <artifactId>javaee-api</artifactId>            <version>7.0</version>            <scope>provided</scope>        </dependency>        <dependency>            <groupId>commons-codec</groupId>            <artifactId>commons-codec</artifactId>            <version>1.10</version>        </dependency>        <dependency>            <groupId>org.hibernate</groupId>            <artifactId>hibernate-entitymanager</artifactId>            <version>4.3.7.Final</version>        </dependency>    </dependencies>    <build>        <!-- Set the name of the war, used as the context root when the app             is deployed -->        <finalName>${project.artifactId}</finalName>        <plugins>            <plugin>                <artifactId>maven-war-plugin</artifactId>                <version>${version.war.plugin}</version>                <configuration>                    <failOnMissingWebXml>false</failOnMissingWebXml>                </configuration>            </plugin>            <plugin>                <groupId>org.wildfly.plugins</groupId>                <artifactId>wildfly-maven-plugin</artifactId>                <version>${version.wildfly.maven.plugin}</version>                <executions>                    <execution>                        <id>cleanApp</id>                        <phase>install</phase>                        <goals>                            <goal>undeploy</goal>                        </goals>                        <configuration>                            <ignoreMissingDeployment>true</ignoreMissingDeployment>                            <hostname>${wildfly.hostname}</hostname>                            <username>${wildfly.username}</username>                            <password>${wildfly.password}</password>                            <!--Artifact details-->                            <match-pattern>${project.artifactId}.*</match-pattern>                        </configuration>                    </execution>                    <execution>                        <id>deploy-app</id>                        <phase>install</phase>                        <goals>                            <goal>deploy</goal>                        </goals>                        <configuration>                            <hostname>${wildfly.hostname}</hostname>                            <username>${wildfly.username}</username>                            <password>${wildfly.password}</password>                        </configuration>                    </execution>                    <execution>                        <id>deploy-driver</id>                        <phase>install</phase>                        <configuration>                            <groupId>${mysql.driver.groupId}</groupId>                            <artifactId>${mysql.driver.artifactId}</artifactId>                            <name>mysql-connector-java-${mysql.driver.version}-bin.jar</name>                        </configuration>                        <goals>                            <goal>deploy-artifact</goal>                        </goals>                    </execution>                    <!-- Add a data source -->             <!-- <execution>                        <id>add-datasource</id>                        <phase>install</phase>                        <goals>                            <goal>add-resource</goal>                        </goals>                        <configuration>                            <resources>                                <resource>                                    <properties>                                        <jndi-name>java:jboss/datasources/WS</jndi-name>                                        <enabled>true</enabled>                                        <connection-url>jdbc:mysql://localhost:3306/WSDatabase</connection-url>                                        <driver-class>com.mysql.jdbc.Driver</driver-class>                                        <driver-name>mysql-connector-java-${mysql.driver.version}-bin.jar</driver-name>                                        <user-name>root</user-name>                                        <password>root</password>                                    </properties>                                </resource>                            </resources>                        </configuration>                    </execution>-->                </executions>            </plugin>        </plugins>    </build></project>